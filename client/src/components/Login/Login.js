import React from 'react'

//component

//classes
import classes from "./Login.css"

const Login = (props) => {
    return (
        <div className={classes.Login}>
            <h1>Login Page</h1>
        </div>
    )
}

export default Login
